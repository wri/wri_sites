status: true
dependencies:
  config:
    - field.storage.taxonomy_term.field_map
    - taxonomy.vocabulary.hub_terms
    - taxonomy.vocabulary.languages
    - taxonomy.vocabulary.regions
    - taxonomy.vocabulary.resource_type
    - taxonomy.vocabulary.tags
    - taxonomy.vocabulary.topics_and_subtopics
id: taxonomy_term.hub_terms.field_map
field_name: field_map
entity_type: taxonomy_term
bundle: hub_terms
label: Map
description: 'If a node is tagged with this term on the Hub, instead tag the node with these terms on this site.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      languages: languages
      regions: regions
      resource_type: resource_type
      tags: tags
      topics_and_subtopics: topics_and_subtopics
    sort:
      field: name
      direction: asc
    auto_create: false
    auto_create_bundle: languages
field_type: entity_reference
