langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_zoom_registration_form
    - node.type.event
  module:
    - webform
id: node.event.field_zoom_registration_form
field_name: field_zoom_registration_form
entity_type: node
bundle: event
label: 'Zoom Registration Form'
description: "You can create a new webform by <a href=\"/admin/structure/webform/manage/zoom_default/duplicate\" target=\"_blank\">duplicating the \"Zoom (default)\" form here</a>.\r\nYou can set the time the webform will become disabled using the \"Zoom Registration Form settings\" and setting \"Scheduled\" to end at the time you wish to close registrations."
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:webform'
  handler_settings:
    target_bundles: null
    auto_create: false
field_type: webform
