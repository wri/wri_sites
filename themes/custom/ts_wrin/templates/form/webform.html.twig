{#
/**
 * @file
 * Theme implementation for a 'webform' element.
 *
 * Adds an anchor link to the top of zoom forms so they can be linked to directly.
 */
#}
{% if element["elements"]["webinar_id"] %}
<a id="register"></a>
{% endif %}
{% include "@webform/webform.html.twig" %}
