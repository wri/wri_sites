{#
/**
 * @file
 * Theme implementation for a 'webform' element.
 *
 * Adds an anchor link to the top of the form so it can be linked to directly.
 */
#}
<a id="webform-{{ element["#webform_id"] }}"></a>
{% include "@webform/webform.html.twig" %}
