{#
/**
 * @file
 * Default theme implementation to display a Social Media Site paragraph.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
  'paragraph--type--' ~ paragraph.bundle|clean_class,
]
%}
{% set social_site = content.field_site.0['#taxonomy_term'].getName() %}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}
      {% if content.field_site.0 and content.field_text.0 %}
        <div class="social-icon-wrapper {{ social_site|lower }}">{{ content.field_site.0 }}</div>
        {% if paragraph.field_link.0.url %}
          <a href="{{ paragraph.field_link.0.url }}" class="handle" >{{ content.field_text.0 }}</a>
        {% endif %}
      {% elseif paragraph.field_link.0.url %}
        <a href="{{ paragraph.field_link.0.url }}"><div class="social-icon-wrapper {{ social_site|lower }}">{{ content.field_site.0 }}</div></a>
      {% endif %}
    {% endblock %}
  </div>
{% endblock paragraph %}
