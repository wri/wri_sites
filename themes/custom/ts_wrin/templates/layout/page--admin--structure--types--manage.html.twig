{#
/**
 * @file
 * Admin pages that use the TS_WRIN theme.
 *
 * We're making the assumption that if you're looking at the frontend theme in the backend,
 You're probably on layout builder. If there are cases where this is not true or this theme isn't desired,
 where this squishes the page unneccessarily, make this template more specific.
 */
#}
{% extends "page.html.twig" %}
{% set is_node = true %}
